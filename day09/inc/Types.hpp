#pragma once
#include <vector>
#include <string>
#include <cstdint>
#include <limits>

#define FREE_SPACE std::numeric_limits<uint64_t>::max()
#define RANGE std::ranges

using Input = std::string;
using Data = std::vector<uint64_t>;
